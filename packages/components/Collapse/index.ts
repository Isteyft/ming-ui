import Collapse from './Collapse.vue'
import CollapseItem from './CollapseItem.vue'
import { withInstall } from '@baize-ui/utils'

export const BaizeCollapse = withInstall(Collapse)
export const BaizeCollapseItem = withInstall(CollapseItem)

export * from "./types";